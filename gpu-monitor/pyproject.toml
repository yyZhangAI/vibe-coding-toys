[project]
name = "gpu-monitor"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "paramiko>=5.0.0",
    "pyyaml>=6.0.3",
    "uvicorn[standard]>=0.47.0",
]

[dependency-groups]
dev = []
